# Install the canonical runtime package from the sibling DewieOps checkout.
# Keep both repositories under the same parent directory, as on the office box.
-e ../../DewieOps
fastapi>=0.110
uvicorn[standard]>=0.29
requests>=2.31
pypdf>=5,<7
